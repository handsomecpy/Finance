# Finance

## 本地运行
直接在浏览器中打开 `index.html` 即可使用计算器。

## 同步到 GitHub
此环境无法直接访问你的 GitHub 账号，因此不会自动推送提交。完成修改并在本地检查 `git status` 与 `git log` 后，可在终端中执行以下命令，将当前分支同步到远程仓库：

```bash
git push origin <你的分支名>
```

将 `<你的分支名>` 替换成实际分支，例如 `main` 或 `work`。命令执行成功后，刷新 GitHub 页面即可看到最新的更改。
